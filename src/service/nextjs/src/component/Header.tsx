import { AppBar, Box, Button, IconButton, Stack, Toolbar, Typography } from '@mui/material';
import MenuIcon from '@mui/icons-material/Menu';

const Header = () => {
	return (
		<Box bgcolor={'blue'} width={'100%'} height={'40px'}>
			헤더
		</Box>
	);
};

export default Header;
