import { css } from '@emotion/css';
import { PageStyle } from 'page/page.style';

export const InfoPageStyle = css(PageStyle, {
	display: 'flex',
	flexDirection: 'column',
	alignItems: 'center',
	justifyContent: 'center',
	gap: '2rem',
});
